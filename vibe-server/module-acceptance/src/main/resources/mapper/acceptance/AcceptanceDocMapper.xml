<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vibe.acceptance.mapper.AcceptanceDocMapper">

    <resultMap id="docVoMap" type="com.vibe.acceptance.vo.AcceptanceDocVO">
        <id property="id" column="id"/>
        <result property="taskId" column="task_id"/>
        <result property="projectId" column="project_id"/>
        <result property="projectName" column="project_name"/>
        <result property="docType" column="doc_type"/>
        <result property="name" column="name"/>
        <result property="fileUrl" column="file_url"/>
        <result property="fileSize" column="file_size"/>
        <result property="docVersion" column="doc_version"/>
        <result property="uploaderId" column="uploader_id"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="docColumns">
        t.id, t.task_id, t.project_id, t.doc_type, t.name, t.file_url, t.file_size,
        t.doc_version, t.uploader_id, t.remark, t.create_time, t.update_time,
        p.project_name AS project_name
    </sql>

    <select id="selectDocPage" resultMap="docVoMap">
        SELECT <include refid="docColumns"/>
        FROM acceptance_doc t
        LEFT JOIN project p ON p.id = t.project_id AND p.deleted = 0
        <where>
            t.deleted = 0
            <if test="query.projectId != null">
                AND t.project_id = #{query.projectId}
            </if>
            <if test="query.taskId != null">
                AND t.task_id = #{query.taskId}
            </if>
            <if test="query.docType != null and query.docType != ''">
                AND t.doc_type = #{query.docType}
            </if>
        </where>
        ORDER BY t.create_time DESC
    </select>

    <select id="selectVoById" resultMap="docVoMap">
        SELECT <include refid="docColumns"/>
        FROM acceptance_doc t
        LEFT JOIN project p ON p.id = t.project_id AND p.deleted = 0
        WHERE t.id = #{id} AND t.deleted = 0
    </select>

</mapper>
