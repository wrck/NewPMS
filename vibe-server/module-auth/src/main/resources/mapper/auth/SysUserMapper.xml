<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vibe.auth.dao.SysUserMapper">

    <!-- 认证模块独立 SysUserMapper：仅查询 sys_user 基础字段，不关联角色/组织 -->
    <resultMap id="sysUserEntityMap" type="com.vibe.system.entity.SysUserEntity">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="realName" column="real_name"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="avatar" column="avatar"/>
        <result property="status" column="status"/>
        <result property="tenantType" column="tenant_type"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="orgId" column="org_id"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findByUsername" resultMap="sysUserEntityMap">
        SELECT id, username, password, real_name, phone, email, avatar,
               status, tenant_type, tenant_id, org_id, last_login_time, create_time, update_time
        FROM sys_user
        WHERE username = #{username} AND deleted = 0
    </select>

    <select id="findByPhone" resultMap="sysUserEntityMap">
        SELECT id, username, password, real_name, phone, email, avatar,
               status, tenant_type, tenant_id, org_id, last_login_time, create_time, update_time
        FROM sys_user
        WHERE phone = #{phone} AND deleted = 0
    </select>

</mapper>
