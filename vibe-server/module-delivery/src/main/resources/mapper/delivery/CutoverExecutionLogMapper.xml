<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vibe.delivery.mapper.CutoverExecutionLogMapper">

    <resultMap id="logVoMap" type="com.vibe.delivery.vo.CutoverExecutionLogVO">
        <id property="id" column="id"/>
        <result property="planId" column="plan_id"/>
        <result property="stepId" column="step_id"/>
        <result property="stepName" column="step_name"/>
        <result property="operatorId" column="operator_id"/>
        <result property="operatorName" column="operator_name"/>
        <result property="action" column="action"/>
        <result property="logTime" column="log_time"/>
        <result property="logContent" column="log_content"/>
        <result property="logLevel" column="log_level"/>
    </resultMap>

    <sql id="logColumns">
        cel.id, cel.plan_id, cel.step_id, cel.operator_id, cel.operator_name,
        cel.action, cel.log_time, cel.log_content, cel.log_level, cel.create_time,
        cs.step_name
    </sql>

    <sql id="logJoins">
        LEFT JOIN cutover_step cs ON cs.id = cel.step_id AND cs.deleted = 0
    </sql>

    <select id="selectLogVoListByPlanId" resultMap="logVoMap">
        SELECT <include refid="logColumns"/>
        FROM cutover_execution_log cel
        <include refid="logJoins"/>
        WHERE cel.plan_id = #{planId}
        ORDER BY cel.log_time ASC
    </select>

</mapper>
