<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vibe.device.mapper.DeviceStatusLogMapper">

    <select id="selectLogListByDevice" resultType="com.vibe.device.entity.DeviceStatusLogEntity">
        SELECT id, device_id, from_status, to_status, operator_id, remark,
               create_by, create_time, update_by, update_time, deleted
        FROM device_status_log
        WHERE deleted = 0 AND device_id = #{deviceId}
        ORDER BY create_time DESC, id DESC
    </select>

</mapper>
