<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vibe.project.mapper.ProjectMilestoneMapper">

    <select id="selectByProjectId" resultType="com.vibe.project.entity.ProjectMilestoneEntity">
        SELECT id, project_id, milestone_name, planned_date, actual_date,
               deliverables, status, create_by, create_time, update_by, update_time, deleted
        FROM project_milestone
        WHERE project_id = #{projectId} AND deleted = 0
        ORDER BY planned_date
    </select>

</mapper>
