<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vibe.system.mapper.SysUserRoleMapper">

    <delete id="deleteByUserId">
        UPDATE sys_user_role SET deleted = 1
        WHERE user_id = #{userId} AND deleted = 0
    </delete>

    <select id="countByRoleId" resultType="java.lang.Long">
        SELECT COUNT(1) FROM sys_user_role
        WHERE role_id = #{roleId} AND deleted = 0
    </select>

</mapper>
